{
  "nbformat": 4,
  "nbformat_minor": 0,
  "metadata": {
    "colab": {
      "provenance": [],
      "authorship_tag": "ABX9TyMYAJVBuOx0CTOaeL1z3eUt",
      "include_colab_link": true
    },
    "kernelspec": {
      "name": "python3",
      "display_name": "Python 3"
    },
    "language_info": {
      "name": "python"
    }
  },
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": {
        "id": "view-in-github",
        "colab_type": "text"
      },
      "source": [
        "<a href=\"https://colab.research.google.com/github/Awais-182/My-Python-Projects/blob/main/basic-arithmetic-calculator/v8_master_engine\" target=\"_parent\"><img src=\"https://colab.research.google.com/assets/colab-badge.svg\" alt=\"Open In Colab\"/></a>"
      ]
    },
    {
      "cell_type": "markdown",
      "source": [
        "# 👑 Basic Arithmetic Calculator – Version 8: Master Engine (The Ultimate Edition)\n",
        "\n",
        "| Metadata Attribute | Technical Details |\n",
        "| :--- | :--- |\n",
        "| **Repository Directory** | `basic-arithmetic-calculator` |\n",
        "| **Notebook File** | `calculator_v8_master_engine.ipynb` |\n",
        "| **Release Version** | `v8.0-FINAL` |\n",
        "| **Author** | Awais |\n",
        "| **Language & Runtime** | Python 3.x (CPython Core) |\n",
        "| **Architecture** | Dynamic Multi-Branch Conditional Execution Engine |\n",
        "\n",
        "---\n",
        "\n",
        "## 📌 Executive Overview & Milestone Significance\n",
        "\n",
        "Version 8 represents the **capstone release** of the `python-program-vault` Basic Arithmetic Calculator series. While Versions 1 through 7 isolated individual mathematical operators (`+`, `-`, `*`, `/`, `%`, `**`, `//`) inside standalone scripts, Version 8 unifies all seven foundational arithmetic operations into a **single, coherent execution engine**.\n",
        "\n",
        "### 🎯 Key Architectural Objectives:\n",
        "1. **Dynamic Routing:** Route user inputs to specific calculation branches dynamically at runtime.\n",
        "2. **Dual-Input Compatibility:** Support both natural language string keywords (e.g., `\"Addition\"`) and mathematical operator symbols (e.g., `\"+\"`).\n",
        "3. **Fault Isolation:** Standardize user prompts and centralize calculation handling to prevent invalid states.\n",
        "4. **String Normalization:** Apply built-in string transformation methods (`.capitalize()`) to deliver flexible user input interpretation."
      ],
      "metadata": {
        "id": "-gRUBBRYvz4i"
      }
    },
    {
      "cell_type": "markdown",
      "source": [
        "## 📐 System Architecture & Program Flow\n",
        "\n",
        "The diagram below maps the execution lifecycle of the **Master Calculator Engine** from initial memory allocation to final output termination:\n",
        "\n",
        "```text\n",
        "[User Execution Request]\n",
        "          │\n",
        "          ▼\n",
        "┌──────────────────────────┐\n",
        "│ Step 1: Input Phase      │  --> Capture first_num & second_num via int(input())\n",
        "└─────────┬────────────────┘\n",
        "          │\n",
        "          ▼\n",
        "┌──────────────────────────┐\n",
        "│ Step 2: Selection Menu   │  --> Render operations menu & capture choice\n",
        "└─────────┬────────────────┘\n",
        "          │\n",
        "          ▼\n",
        "┌──────────────────────────┐\n",
        "│ Step 3: Decision Engine  │  --> Evaluate choice against 7 conditional branches\n",
        "└─────────┬────────────────┘\n",
        "          │\n",
        "   ┌──────┴───────────────────────────────────────────────────────┐\n",
        "   │                                                              │\n",
        "   ▼                                                              ▼\n",
        "[ Valid Branch Selected ]                                [ Unrecognized Choice ]\n",
        "   │                                                              │\n",
        "   ├──> Execute Target Math Logic                                 └──> Trigger Fallback\n",
        "   ├──> Store Value in result                                        └──> Display Error Notice\n",
        "   └──> Output Result Matrix                                           └──> Graceful Exit"
      ],
      "metadata": {
        "id": "DYZXKwWov6oS"
      }
    },
    {
      "cell_type": "markdown",
      "source": [
        "---\n",
        "\n",
        "### 📥 Step 1: Input Acquisition & Operation Selection\n",
        "\n",
        "#### Technical Mechanics:\n",
        "* **Memory Allocation:** Two integer memory locations (`first_num` and `second_num`) are instantiated via explicit type coercion wrapping the `input()` function.\n",
        "* **Console UI Rendering:** A formatted console menu presents the user with supported operational modes and syntax options.\n",
        "* **Selection Capture:** The string variable `choice` receives raw console input for validation against internal conditional thresholds.\n",
        "\n",
        "#### Code Snippet:\n",
        "```python\n",
        "first_num = int(input(\"Enter The First Number: \"))\n",
        "second_num = int(input(\"Enter The Second Number: \"))\n",
        "\n",
        "print(\"\")\n",
        "print(\"Choose an arithmatic operation to apply:\")\n",
        "print(\"Addition(+), Subtraction(-),Multiplication(*),Division(/),Floor division (//),Exponentation(**) or Module(%)\")\n",
        "print(\"\")\n",
        "\n",
        "choice = input(\"Choose an operation: \")"
      ],
      "metadata": {
        "id": "UodKKhkqwQZz"
      }
    },
    {
      "cell_type": "markdown",
      "source": [
        "***\n",
        "\n",
        "### Step 2 – Decision Engine & Execution Logic**\n",
        "\n",
        "```markdown\n",
        "## ⚙️ Step 2: Conditional Decision Engine & Execution\n",
        "\n",
        "### Technical Mechanics:\n",
        "* **Short-Circuit Evaluation:** Uses multi-branch `if-elif-else` control structures to evaluate boolean logical OR conditions (`or`).\n",
        "* **String Normalization:** Employs `.capitalize()` to convert user-typed lower-case strings (e.g., `\"addition\"`) to title-case (`\"Addition\"`), enabling dual matching against string labels or symbol primitives (`\"+\"`).\n",
        "* **Branch Fallback:** An overarching `else` block catches unexpected input patterns and prevents unhandled script exceptions.\n",
        "\n",
        "```python\n",
        "if choice == \"addition\".capitalize() or choice == \"+\":\n",
        "    result = first_num + second_num\n",
        "    print(result)\n",
        "elif choice == \"subtraction\".capitalize() or choice == \"-\":\n",
        "    result = first_num - second_num\n",
        "    print(result)\n",
        "# ... [Full decision tree executed in code cell below]"
      ],
      "metadata": {
        "id": "rtJJFGcRwVvX"
      }
    },
    {
      "cell_type": "code",
      "execution_count": null,
      "metadata": {
        "colab": {
          "base_uri": "https://localhost:8080/"
        },
        "id": "fFZqNaf8vyWE",
        "outputId": "ad11709e-3eb8-4a4e-c75b-3d91085a324e"
      },
      "outputs": [
        {
          "output_type": "stream",
          "name": "stdout",
          "text": [
            "Enter The First Number: 2\n",
            "Enter The Second Number: 9\n",
            "\n",
            "Choose an arithmatic operation to apply:\n",
            "Addition(+), Subtraction(-),Multiplication(*),Division(/),Floor division (//),Exponentation(**) or Module(%)\n",
            "\n",
            "Choose an operation: +\n",
            "Your Result is: 11\n"
          ]
        }
      ],
      "source": [
        "\n",
        "### The Master Piece Engine (Production Code)\n",
        "# ==============================================================================\n",
        "# 🧮 PROGRAM: Basic Arithmetic Calculator - Master Piece Version (v8.0)\n",
        "# 🎯 AUTHOR: Awais\n",
        "# 🛠️ WORKING: It let the user decide two numbers and let User choose the operation to be applied on that two number.\n",
        "# ==============================================================================\n",
        "\n",
        "# Step 1: Input Acquisition\n",
        "first_num = int(input(\"Enter The First Number: \"))\n",
        "second_num = int(input(\"Enter The Second Number: \"))\n",
        "\n",
        "print(\"\")\n",
        "print(\"Choose an arithmatic operation to apply:\")\n",
        "print(\"Addition(+), Subtraction(-),Multiplication(*),Division(/),Floor division (//),Exponentation(**) or Module(%)\")\n",
        "print(\"\")\n",
        "\n",
        "# Step 2: Operational Choice Capture\n",
        "choice = input(\"Choose an operation: \")\n",
        "\n",
        "# Step 3: Master Decision Engine & Execution Pipeline\n",
        "if choice == \"addition\".capitalize() or choice == \"+\":\n",
        "    result = first_num + second_num\n",
        "    print(\"Your Result is:\",result)\n",
        "elif choice == \"subtraction\".capitalize() or choice == \"-\":\n",
        "    result = first_num - second_num\n",
        "    print(\"Your Result is:\",result)\n",
        "elif choice == \"multipilication\".capitalize() or choice == \"*\":\n",
        "    result = first_num * second_num\n",
        "    print(\"Your Result is:\",result)\n",
        "elif choice == \"division\".capitalize() or choice == \"/\":\n",
        "    result = first_num / second_num\n",
        "    print(\"Your Result is:\",result)\n",
        "elif choice == \"floor division\".capitalize() or choice == \"//\":\n",
        "    result = first_num // second_num\n",
        "    print(\"Your Result is:\",result)\n",
        "elif choice == \"exponentation\".capitalize() or choice == \"**\":\n",
        "    result = first_num ** second_num\n",
        "    print(\"Your Result is:\",result)\n",
        "elif choice == \"modulo\".capitalize() or choice == \"%\":\n",
        "    result = first_num % second_num\n",
        "    print(\"Your Result is:\",result)\n",
        "else:\n",
        "    print(\"Invalid operation selected.\")"
      ]
    },
    {
      "cell_type": "markdown",
      "source": [
        "## 📊 Version Evolution & Technical Breakdown\n",
        "\n",
        "The table below contrasts the technical capabilities of early single-purpose releases against this ultimate Master Version:\n",
        "\n",
        "| Technical Feature | Versions 1–7 (Individual Notebooks) | Version 8 (Master Piece Engine) |\n",
        "| :--- | :--- | :--- |\n",
        "| **Supported Operations** | 1 operator per file | **7 unified operators** (`+`, `-`, `*`, `/`, `//`, `**`, `%`) |\n",
        "| **Input Parsing Syntax** | Single fixed path | **Dual-input matching** (Symbol OR Name) |\n",
        "| **String Handling** | None | `.capitalize()` normalization |\n",
        "| **Control Flow Complexity** | Linear execution ($O(1)$ path) | Multi-branch decision tree ($O(N)$ matching) |\n",
        "| **Error Handling Scope** | Basic type conversion crashes | Basic branch validation (`else` catch-all) |\n",
        "\n",
        "---\n",
        "\n",
        "## ⚠️ Known Limitations & Enterprise Upgrade Path\n",
        "\n",
        "| Issue / Constraint | Technical Root Cause | Future Enterprise Optimization (`v9.0`) |\n",
        "| :--- | :--- | :--- |\n",
        "| **Type Rigidity** | `int()` strictly rejects real floating-point numbers. | Refactor parser using `float()` type casting or `decimal.Decimal`. |\n",
        "| **Zero Division Crash** | Unhandled zero denominator during `/`, `//`, or `%`. | Enforce `try-except ZeroDivisionError` blocks. |\n",
        "| **Case Matching Limits** | `.capitalize()` fails for fully uppercase strings (e.g., `\"ADDITION\"`). | Standardize using `.lower()` or `.strip()`. |\n",
        "| **Lifecycle Termination** | Single-run execution pipeline. | Enclose core execution logic within a `while True` main loop. |"
      ],
      "metadata": {
        "id": "IuhuAIj3wt_4"
      }
    },
    {
      "cell_type": "code",
      "source": [],
      "metadata": {
        "id": "W8r4xqQwwqLd"
      },
      "execution_count": null,
      "outputs": []
    }
  ]
}